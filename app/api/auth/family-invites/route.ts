import{NextResponse}from'next/server';
import{z}from'zod';
import{requireApiUser}from'@/lib/supabase/server';

const input=z.object({familyId:z.string().uuid(),intendedRole:z.enum(['PARENT','YOUTH'])});

export async function POST(request:Request){try{const body=input.parse(await request.json()),{admin,user}=await requireApiUser(request),membership=await admin.from('family_members').select('role').eq('family_id',body.familyId).eq('profile_id',user.id).maybeSingle();if(membership.error||!membership.data)return NextResponse.json({error:'Você não pertence a esta família.'},{status:403});if(membership.data.role==='YOUTH'&&body.intendedRole!=='PARENT')return NextResponse.json({error:'O jovem pode convidar um responsável.'},{status:403});for(let attempt=0;attempt<3;attempt++){const code=crypto.randomUUID().replaceAll('-','').slice(0,6).toUpperCase(),created=await admin.from('family_invites').insert({family_id:body.familyId,code,youth_name:'Novo membro',youth_age:14,relationship:body.intendedRole==='PARENT'?'Responsável':'Jovem',intended_role:body.intendedRole}).select('link_token,code,intended_role').single();if(!created.error&&created.data)return NextResponse.json({ok:true,linkToken:created.data.link_token,code:created.data.code,intendedRole:created.data.intended_role});if(created.error.code!=='23505')throw created.error}return NextResponse.json({error:'Não foi possível gerar um código único. Tente novamente.'},{status:409})}catch{return NextResponse.json({error:'Não foi possível criar o convite agora.'},{status:400})}}
