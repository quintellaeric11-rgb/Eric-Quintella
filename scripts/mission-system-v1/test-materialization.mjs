import{materializeMission,validateMaterializedMission}from'../../lib/mission-system-v1/mission-materializer.mjs';import{validateCompletion}from'../../lib/mission-system-v1/completion-validator.mjs';import{harness,assert}from'./test-helpers.mjs';const h=harness('materialization');
h.test('M05 não inventa oferta',()=>assert.equal(materializeMission('M05',{}).code,'NEEDS_USER_INPUT'));
h.test('rejeita fato gerado',()=>assert.ok(materializeMission('M05',{offer_delivery:{value:'oferta',source:'MISSION_OUTPUT',generated:true},target_person_type:{value:'família',source:'FAMILY_RULES'}}).errors.some(x=>x.startsWith('GENERATED_FACT_FORBIDDEN'))));
h.test('conteúdo canônico materializa sem alterar pedagogia',()=>{const r=materializeMission('M23',{goal_name:{value:'Projeto',source:'GOAL_STATE'}});assert.equal(r.code,'MISSION_SELECTED');assert.equal(r.mission.completionRuleId,'CR_M23');assert.equal(validateMaterializedMission(r.mission).valid,true)});
h.test('conclusão não exige sucesso externo em M05',()=>{const r=validateCompletion('M05',{actionsCompleted:['real_offer_presented','response_recorded','learning_recorded'],evidence:[{type:'STRUCTURED_INPUT',value:'A pessoa disse não'}],facts:{third_party_response:{value:'não',source:'USER_INPUT_CURRENT'}}});assert.equal(r.valid,true);assert.equal(r.negativeResponseCanComplete,true)});
h.test('conclusão rejeita sucesso fabricado',()=>assert.equal(validateCompletion('M01',{evidence:[{type:'TEXT',value:'teste'}],fabricatedSuccess:true}).valid,false));h.finish();
