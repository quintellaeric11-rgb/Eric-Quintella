import{MISSION_CONTENT_INDEX}from'../../data/mission-system-v1/mission-content-index.mjs';import{harness,assert}from'./test-helpers.mjs';const h=harness('editorial-rules');h.test('45 conteúdos canônicos carregados e hashados',()=>{assert.equal(Object.keys(MISSION_CONTENT_INDEX).length,45);for(const x of Object.values(MISSION_CONTENT_INDEX)){assert.match(x.source,/KONKI_MISSION_BIBLE_V1/);assert.equal(x.contentStatus,'CANONICAL_CONTENT_LOADED');assert.match(x.contentHash,/^[a-f0-9]{64}$/)}});h.finish();
